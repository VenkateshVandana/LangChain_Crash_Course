[project]
name = "langchain-crash-course"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "VenkateshVandana", email = "venkivandana14@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.3.18",
    "langchain-core>=1.6.1",
    "langchain-groq>=1.1.3",
    "python-dotenv>=1.2.3",
]

[project.scripts]
langchain-crash-course = "langchain_crash_course:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
